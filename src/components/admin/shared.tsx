"use client";
// Re-export everything from the new shared/index module so both
// `@/components/admin/shared` and `@/components/admin/shared/index`
// resolve to the same set of components.
export * from "./shared/index";
